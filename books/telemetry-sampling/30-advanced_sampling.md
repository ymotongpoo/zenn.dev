---
title: "高度な戦略：動的サンプリングとアグリゲーション"
---

## 動的サンプリング（Adaptive Sampling）

トラフィックの変動が激しいサービスでは、固定のサンプリング率（例: 1%）を設定していると、スパイク時にデータ量が爆発したり、逆に深夜帯にデータが少なすぎて分析が困難になったりします。これを解決するのが **動的サンプリング（Adaptive Sampling）** です。

### 特徴
* **流量に応じた自動調整**: 現在のトラフィック量をリアルタイム（または数分単位）で監視し、目標とするデータ量（例: 500スパン/秒）に収まるよう、サンプリング率を自動的に増減させます。
* **アルゴリズムの例**: 
    * **Refinery (Honeycomb)**: 特定のキー（HTTPステータスコード、URL、顧客IDなど）ごとにサンプリング率を動的に計算します。
    * **EMA (Exponential Moving Average)**: 指数移動平均を用いて将来のトラフィックを予測し、サンプリング率を滑らかに調整します。

## アグリゲーション（集約）

サンプリングは「データを捨てる」行為ですが、**アグリゲーション（集約）** は「詳細を捨てる代わりに、傾向（統計量）を残す」行為です。

### 生トレースを捨てる代わりにメトリクス化する
すべてのトレースを保存するのはコスト的に不可能でも、すべてのリクエストのレイテンシやエラーの有無をカウントすることは可能です。OpenTelemetry Collectorの `spanmetricsconnector` や各種プロセッサを使用して、スパンからメトリクス（リクエスト数、レイテンシのヒストグラムなど）を事前生成します。
これにより、サンプリングでトレースを0.1%まで絞り込んだとしても、サービス全体の正確な成功率やP99レイテンシをメトリクスとして維持し続けることができます。

## 「4つの黄金シグナル」の維持

サンプリング導入下でも、GoogleのSRE Bookで提唱されている **「4つの黄金シグナル」** を正しく監視し続けるにはコツが必要です。

1.  **レイテンシ (Latency)**: サンプリングされたトレースだけでなく、全量を反映したメトリクスから算出します。
2.  **トラフィック (Traffic)**: サンプリング率（1/N）を用いて、失われたデータを逆算して元の統計量を推定します（Chapter 5詳述）。
3.  **エラー (Errors)**: テイルサンプリングを用いてエラーを含むトレースは優先的に（できれば100%）保存するように設定します。
4.  **サチュレーション (Saturation)**: キャパシティの限界を示すシグナルは、サンプリングの影響を受けないシステムメトリクス（CPU使用率、メモリ、コネクション数など）と組み合わせて把握します。

サンプリングは単なるデータ削減手段ではなく、監視のニーズに合わせて「情報の密度」を最適化する戦略なのです。
